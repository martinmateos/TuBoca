<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="TuBoca.Paginas.Universidades"
             x:Name="Universidad">

    <CarouselView BindingContext="{x:Reference Name=Universidad}" ItemsSource="{Binding ListUnis}" x:Name="CaurouselUni">
        <CarouselView.ItemTemplate>
            <DataTemplate>
                <StackLayout>
                    <Label Text="{Binding MostrarDatos}" LineBreakMode="CharacterWrap"/>
                </StackLayout>
            </DataTemplate>
        </CarouselView.ItemTemplate>
    </CarouselView>
</ContentPage>